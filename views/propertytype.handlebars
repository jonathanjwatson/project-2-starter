{{>navbar}}
<div class="container">
{{#each listings}}

<div class="row">
    <div class="col m4 s12">
        <div>
            <div class="card ">
               
            <a href="/renter">
                <div class="card-image">
                    <img alt={{name}} src={{img}}/> 
                </div>
            </a>    
            </div>
        </div>
  </div>
<div class="col m8">

   <h2 class="card-title">{{name}}</h2>
                <p>{{description}}</p> 

</div>
</div>

{{/each}}
</div>